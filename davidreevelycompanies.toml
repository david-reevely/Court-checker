# David Reevely — Ontario Courts Monitor watchlist
#
# This file now draws its company list from the shared master roster
# (david-reevely/beat-companies) instead of local [[companies]] blocks.
# The single line  companies_source = "beat_companies"  is what switches it on.
# Add or remove companies in the master companies.toml, not here.
#
# Claire's file (and any other reporter's) is unaffected: without that line,
# a reporter file keeps using its own [[companies]] blocks exactly as before.

[reporter]
name = "David Reevely"
email = "david.reevely@thelogic.co"
companies_source = "beat_companies"
